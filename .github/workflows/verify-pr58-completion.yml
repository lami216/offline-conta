name: Verify PR58 completion

on:
  push:
    branches:
      - fix/opening-stock-integrity-20260910
    paths:
      - .ci/run-pr58-completion

permissions:
  contents: write

jobs:
  complete:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/opening-stock-integrity-20260910
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: node scripts/pr58-complete.mjs
      - name: Persist completion patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Apply PR58 completion draft"
            git push origin HEAD:fix/opening-stock-integrity-20260910
          fi
      - run: npm ci
      - run: npm test
      - run: npx tsc --noEmit
      - run: npm run lint
      - run: npm run build
      - run: git diff --check
