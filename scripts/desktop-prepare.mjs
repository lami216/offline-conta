import {cp,mkdir,rm} from 'node:fs/promises';const out='desktop-dist/app';await rm('desktop-dist',{recursive:true,force:true});await mkdir(out,{recursive:true});await cp('.next/standalone',out,{recursive:true});await mkdir(`${out}/.next`,{recursive:true});await cp('.next/static',`${out}/.next/static`,{recursive:true});await cp('public',`${out}/public`,{recursive:true});await cp('node_modules/sql.js/dist/sql-wasm.wasm',`${out}/node_modules/sql.js/dist/sql-wasm.wasm`,{recursive:false});
