import sharp from 'sharp';import pngToIco from 'png-to-ico';import {mkdir,writeFile,rm} from 'node:fs/promises';await mkdir('build',{recursive:true});const png='build/alkarna-icon-source.png';await sharp('public/alkarna-logo.png').resize(256,256,{fit:'contain',background:{r:0,g:0,b:0,alpha:0}}).png().toFile(png);await writeFile('build/alkarna.ico',await pngToIco(png));await rm(png,{force:true});
