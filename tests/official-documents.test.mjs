import test from "node:test";import assert from "node:assert/strict";import {readFileSync} from "node:fs";
import {DEFAULT_INVOICE_BRANDING,validateInvoiceBranding} from "../lib/invoice-branding.ts";
const app=readFileSync(new URL("../app/conta-app.tsx",import.meta.url),"utf8"),css=readFileSync(new URL("../app/globals.css",import.meta.url),"utf8");
test("branding defaults and strict validation",()=>{assert.deepEqual(DEFAULT_INVOICE_BRANDING,{storeName:"الكرنه",storeLogoDataUrl:"",storePhone:"",storeAddress:"",registrationNumber:"",taxNumber:"",footerNote:"",nameFont:"tahoma",nameFontSize:24,nameFontWeight:800});assert.equal(validateInvoiceBranding({storeName:" فيه البركة ",nameFont:"tahoma",nameFontSize:26,nameFontWeight:800}).storeName,"فيه البركة");for(const invalid of [{storeName:"",nameFont:"tahoma",nameFontSize:24,nameFontWeight:800},{storeName:"x",nameFont:"remote",nameFontSize:24,nameFontWeight:800},{storeName:"x",nameFont:"arial",nameFontSize:40,nameFontWeight:800}])assert.throws(()=>validateInvoiceBranding(invalid))});
test("official presentations preserve operation language",()=>{for(const label of ["فاتورة بيع","فاتورة شراء","سند مصروف","سند قبض","سند صرف","سند مقاصة","تسوية حساب","سند تحويل مخزون","سند تصحيح مخزون","سند تحويل بين الحسابات","سند إيداع","سند سحب","سند تصحيح رصيد"])assert.ok(app.includes(label),label);assert.match(app,/function OfficialRecordSheet/);assert.match(app,/buildDocumentPresentation/);assert.match(app,/buildFinancialPresentation/)});
test("document print is a body portal with display isolation",()=>{assert.match(app,/createPortal\(<div className="document-print-portal"/);assert.match(css,/body>\*\{display:none!important\}/);assert.doesNotMatch(css,/print-document-mode[^}]*visibility\s*:\s*hidden/);assert.match(css,/@page invoice\{size:A4 portrait/);assert.match(css,/thead\{display:table-header-group\}/);assert.match(css,/\.reports-workspace\{page:report\}/)});
